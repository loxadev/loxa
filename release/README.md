# Local release output

Generated release output is local-only and belongs under `release/out/`, which
is ignored by Git. The scripts in this repository can build and validate local
candidate artifacts, but cannot publish them to any registry or release host.
