# loxa

Loxa measures what a machine can actually run — real benchmarks of speed, memory, and tool-calling reliability on the user's own hardware, not estimates — then auto-configures and serves the best setup through OpenAI/Anthropic-compatible APIs, with automatic cloud-to-local failover.
