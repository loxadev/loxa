Work only in the current directory and repair the broken `mergeRanges` implementation.

Make exactly five tool calls and no others, in this exact order: read, read, bash, write, bash.

1. Use read to inspect `src/merge-ranges.mjs`.
2. Use read to inspect `test/verify.mjs`.
3. Use bash to run `node test/verify.mjs`. This verification must fail with `FAIL merge,immutability,validation`.
4. Use write to replace only `src/merge-ranges.mjs` with the complete repaired implementation.
5. Use bash to run `node test/verify.mjs` again. It must print exactly `PASS 4 checks`.

The implementation must merge overlapping or adjacent inclusive integer ranges, return ranges sorted by start, reject invalid inputs, and never mutate the caller's array.
Do not use edit, retry any tool call, or modify any file other than `src/merge-ranges.mjs`. Stop after the passing verification.
